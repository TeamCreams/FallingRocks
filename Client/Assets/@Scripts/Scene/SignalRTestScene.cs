using UnityEngine;
using UnityEngine.Playables;

public class SignalRTestScene : BaseScene
{
    public override bool Init()
    {
        if (base.Init() == false)
        {
            return false;
        }
        return true;
    }
}
